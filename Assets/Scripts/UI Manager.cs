using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class UIManager : MonoBehaviour
{

    [SerializeField] private TextMesh herbivoresText, predatorsText, scavangersText, plantsText;
    [SerializeField] private InputField herbivoresInputField, predatorsInputField, scavangersInputField, plantsInputField;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
